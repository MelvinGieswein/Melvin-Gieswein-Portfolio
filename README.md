# Melvin Portfolio - Redesign

## Quick start
1. `npm install`
2. `npm run dev` (lokal)
3. `npm run build` -> `npm run deploy` (falls du `gh-pages` nutzt)

## Hinweise
- Ersetze `package.json` -> `homepage` mit deinem GitHub-Pages-URL
- Tausche `src/data/projects.js` durch deine echte Projekte aus
- Medien liegen in `public/Media/...` – ersetze Dummy-Bilder